<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.szl.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".test.service.TestService"
            android:enabled="true"
            android:exported="true">
        </service>

        <activity android:name=".test.service.TestServiceActivity">
        </activity>
        <activity android:name=".test.mvp.TestMVPActivity">
        </activity>
        <activity android:name=".test.sort.TestSortActivity">
        </activity>
        <activity android:name=".test.fragment.TestFragmentActivity">
        </activity>
        <activity android:name=".test.fragment.SecondActivity">
        </activity>
        <activity android:name=".test.dataBinding.TestDataBindingActivity">
        </activity>
    </application>

</manifest>